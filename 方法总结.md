# 方法总结

## 1. 数据预处理方法

### 1.1 训练前预处理（离线）

| 步骤 | 方法 | 说明 |
|:----:|------|------|
| EXIF 矫正 | 读取 EXIF Orientation 标签，自动旋转至正向 | 消除拍摄设备朝向不一致 |
| 尺寸缩放 | Stretch 拉伸至 640×640 | 匹配模型输入尺寸；同步转换标注框坐标 |
| 格式统一 | 全部转为 JPEG 格式、RGB 三通道 | 颜色空间和格式标准化 |

### 1.2 推理时预处理（在线）

| 方法 | 配置 | 说明 |
|------|------|------|
| Resize | 640px 宽等比例缩放 | 保持宽高比的推理尺寸 |
| CLAHE | 关闭（`PREPROCESS_CLAHE=False`） | 省 ~20ms，暗光场景可按需开启 |
| Color Boost | 关闭（`PREPROCESS_COLOR_BOOST=False`） | NO-标签直接检出违规，无需增强荧光色 |
| 预处理频率 | 每 3 帧执行一次（`PREPROCESS_EVERY_N=3`） | 减少重复 HSV 转换开销 |
| 暗区惩罚 | 推理时移除（仅训练保留 `HELMET_DARK_PENALTY=0.6`） | 省第二次 HSV 转换 |

---

## 2. 数据集扩展方法

| 方法 | 参数 | 效果 |
|------|------|------|
| Mosaic 拼接 | `mosaic=1.0`，前 10 epoch 启用 | 4 图拼接为 1 张，增加小目标多样性和背景复杂度 |
| 水平翻转 | `fliplr=0.5` | 50% 概率左右翻转，增加朝向多样性 |
| HSV 颜色抖动 | `h=0.015, s=0.7, v=0.4` | 模拟不同光照和色彩条件 |
| 尺度缩放 | `scale=0.5` | 50% 范围尺度变化，增强多尺度检测能力 |
| 平移 | `translate=0.1` | 10% 范围内随机平移 |
| Random Erasing | `erasing=0.4` | 40% 概率随机擦除矩形区域，模拟遮挡 |
| RandAugment | `auto_augment=randaugment` | 自动搜索最优增强策略组合 |

---

## 3. 模型改进方法

### 3.1 正负标签联合判断
- 模型同时输出**正标签**（Hardhat/Safety Vest，合规装备）和**负标签**（NO-Hardhat/NO-Safety Vest，违规检出）
- 违规 = NO-* 持续检出 OR 正标签持续缺失（OR 逻辑）
- 清除 = 正标签持续存在 AND NO-* 持续消失（AND 逻辑）
- 相比传统单一推断模式，减少误报和漏报

### 3.2 连续帧防抖
- 连续帧计数器替代 EMA 速率，判断更精确
- 负标签需 8 帧持续检出才告警（防模型误检）
- 正标签缺失仅需 3 帧即告警（高置信快速响应）
- 清除需正标签 5 帧 + 负标签消失 10 帧（慢释放防闪烁）
- 自适应置信度阈值：历史均值 × 50%

### 3.3 中心 ROI 过滤
- 仅评估画面中心 50% 区域，排除边缘遮挡和目标过远的误判

### 3.4 告警去重合并
- 同一人的多个违规类型合并为单条告警
- 清除事件不计入统计

### 3.5 双模型热切换
- Model Registry 管理 RT-DETR 和 Faster R-CNN
- 前端一键切换，引擎自动重载，无需重启服务
